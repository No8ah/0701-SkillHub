This skill assumes the file contains a front comment header with fields like:

- `% FILE: ...`
- `% PATH: ...`
- `% AUTHOR: ...`
- `% LAST MODIFIED: YYYY-MM-DD`

The script updates only these fields and leaves everything else unchanged.
