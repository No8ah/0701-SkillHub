- Example

    - Prompt

        往后缩进两格

    - 之前  

        \textbf{Step3:}\quad 计算处理

        \textbf{Step3:}\quad $\phi_{k}(0)$

            \hspace*{2em} 1. 因此 - $\phi_{k}(0) = f(\boldsymbol{x}_{k})$

        \textbf{Step4:}\quad $\phi_{k}^{'}(0)$

            \hspace*{2em} 1. 求导 - $\phi_{k}^{'}(\alpha)
            = \nabla f(\boldsymbol{x}_{k} + \alpha \boldsymbol{d}_{k})^{T} \boldsymbol{d}_{k}$

            \hspace*{2em} 2. 因此 - $\phi_{k}^{'}(0) 
            = 
            \nabla f(\boldsymbol{x}_{k})^{T} \boldsymbol{d}_{k}$

    - 之后

        \hspace*{2em} \textbf{Step3:}\quad 计算处理

            \hspace*{4em} 1. $\phi_{k}(0)$

                \hspace*{6em} 1.1. 因此 - $\phi_{k}(0) = f(\boldsymbol{x}_{k})$

            \hspace*{4em} 2. $\phi_{k}^{'}(0)$

                \hspace*{6em} 2.1. 求导 - $\phi_{k}^{'}(\alpha)
                = \nabla f(\boldsymbol{x}_{k} + \alpha \boldsymbol{d}_{k})^{T} \boldsymbol{d}_{k}$

                \hspace*{6em} 2.2. 因此 - $\phi_{k}^{'}(0) 
                = 
                \nabla f(\boldsymbol{x}_{k})^{T} \boldsymbol{d}_{k}$
